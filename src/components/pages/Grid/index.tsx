import React from 'react';
import { RouteProps } from 'react-router';


const Grid: React.VFC<Props> = (props) => {
  return (
    <></>
  );
};

interface Props extends RouteProps {}

export default Grid;
